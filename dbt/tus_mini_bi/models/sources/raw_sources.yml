version: 2

sources:
  - name: raw
    description: "Raw synthetic source tables loaded into DuckDB from our CSV extracts"
    schema: raw
    tables:
      - name: students
        description: "Raw students data loaded from CSV"
      - name: programmes
        description: "Raw programmes vatalogue data loaded from CSV"
      - name: enrolments
        description: "Raw enrolments records data loaded from CSV"
      - name: academic_performance
        description: "Raw academic performance data loaded from CSV"

        